name: Deploy Firebase Functions

on:
  push:
    branches:
      - main
      - development
    paths:
      - 'apps/cloud-functions/functions/**'
      - 'packages/shared/**'
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy to"
        required: true
        default: "development"
        type: choice
        options:
          - development
          - production
      debug_mode:
        description: "Enable debug mode"
        required: false
        type: boolean
        default: false

jobs:
  test:
    name: Test & Quality Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Enable Corepack
        run: |
          corepack enable
          corepack prepare npm@11.16.0 --activate

      - name: Install dependencies
        run: npm ci

      - name: Run Biome lint & format check
        run: npm run biome:check -w spendless-functions

      - name: Run unit tests
        run: npm run test -w spendless-functions

      - name: Generate test coverage report
        run: npm run test:coverage -w spendless-functions

      - name: Archive test coverage results
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: apps/cloud-functions/functions/coverage/
          retention-days: 14

  deploy:
    name: Deploy Firebase Functions
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/development' || github.event_name == 'workflow_dispatch'
    environment: ${{ github.ref == 'refs/heads/main' && 'production' || github.event.inputs.environment || 'development' }}
    env:
      ENVIRONMENT: ${{ github.ref == 'refs/heads/main' && 'production' || github.event.inputs.environment || 'development' }}
      DATABASE_ID: ${{ vars.DATABASE_ID }}
      STORAGE_BUCKET: ${{ vars.STORAGE_BUCKET }}
      FIREBASE_PROJECT_ID: ${{ vars.FIREBASE_PROJECT_ID }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Enable Corepack
        run: |
          corepack enable
          corepack prepare npm@11.16.0 --activate

      - name: Install dependencies
        run: npm ci

      - name: Install Firebase CLI
        run: npm install --no-save firebase-tools

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}

      - name: Create Firebase service account file
        run: |
          echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > apps/cloud-functions/functions/spendless-firebase-adminsdk.json

      - name: Create Environment
        run: |
          echo "SENTRY_DSN=${{ secrets.SENTRY_DSN }}" > apps/cloud-functions/functions/.env
          echo "ENVIRONMENT=${{ env.ENVIRONMENT }}" >> apps/cloud-functions/functions/.env
          echo "DEBUG_MODE=${{ github.event.inputs.debug_mode || 'false' }}" >> apps/cloud-functions/functions/.env
          echo "MAILGUN_API_KEY=${{ secrets.MAILGUN_API_KEY }}" >> apps/cloud-functions/functions/.env
          echo "MAILGUN_DOMAIN=${{ secrets.MAILGUN_DOMAIN }}" >> apps/cloud-functions/functions/.env
          echo "DATABASE_ID=${{ env.DATABASE_ID }}" >> apps/cloud-functions/functions/.env
          echo "STORAGE_BUCKET=${{ env.STORAGE_BUCKET }}" >> apps/cloud-functions/functions/.env
          echo "PROJECT_ID=${{ env.FIREBASE_PROJECT_ID }}" >> apps/cloud-functions/functions/.env
          echo "STRIPE_PRICE_ID_MONTHLY=${{ secrets.STRIPE_PRICE_ID_MONTHLY }}" >> apps/cloud-functions/functions/.env
          echo "STRIPE_PRICE_ID_ANNUAL=${{ secrets.STRIPE_PRICE_ID_ANNUAL }}" >> apps/cloud-functions/functions/.env

      - name: Ensure Stripe secrets exist in Secret Manager
        run: |
          echo -n "${{ secrets.STRIPE_SECRET_KEY }}" | npx firebase functions:secrets:set STRIPE_SECRET_KEY --project ${{ env.FIREBASE_PROJECT_ID }} --force || true
          echo -n "${{ secrets.STRIPE_WEBHOOK_SECRET }}" | npx firebase functions:secrets:set STRIPE_WEBHOOK_SECRET --project ${{ env.FIREBASE_PROJECT_ID }} --force || true

      - name: Build functions
        run: npm run build -w spendless-functions

      - name: Deploy to Firebase
        env:
          DATABASE_ID: ${{ env.DATABASE_ID }}
          STORAGE_BUCKET: ${{ env.STORAGE_BUCKET }}
          FIREBASE_PROJECT_ID: ${{ env.FIREBASE_PROJECT_ID }}
        run: |
          npx firebase use ${{ env.FIREBASE_PROJECT_ID }}
          npx firebase deploy --only functions --force
