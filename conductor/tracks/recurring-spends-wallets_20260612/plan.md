# Implementation Plan: Select Wallet for Recurring Spends on Period Creation

**Track ID:** recurring-spends-wallets_20260612
**Spec:** spec.md
**Created:** 2026-06-12
**Status:** [ ] Not Started

## Overview

Add support in the new period creation wizard to let users explicitly assign recurring spends to their newly configured period wallets. When the period is created, the system will update the recurring spends in Firestore with the selected wallet name mapping so that the auto-generated spends are correctly assigned.

## Phase 1: Form State & Logic Updates

Extend the multi-step form hook to support tracking the custom wallet mappings for recurring spends.

### Tasks

- [x] Task 1.1: Add `recurringSpendsWalletMapping` property (`Record<string, string>`, mapping `recurringSpendId` to `walletName`) to `PeriodFormData` interface and its initializer. 4db3bfb
- [x] Task 1.2: Implement `updateRecurringSpendWalletMapping(recurringSpendId: string, walletName: string)` action in `useMultiStepForm` to update the mapping state. b8e0694
- [x] Task 1.3: Create unit tests in `src/pages/spending/modals/periodModal/hooks/useMultiStepForm.test.ts` to verify form state initialization and wallet mapping updates. b59f417

### Verification

- [ ] Run unit tests: `npx vitest run src/pages/spending/modals/periodModal/hooks/useMultiStepForm.test.ts`

## Phase 2: UI Component Updates

Update the step components inside the period modal to allow editing and viewing selected wallets.

### Tasks

- [ ] Task 2.1: Update `StepRecurringExpenses.tsx` to render a wallet select dropdown for each recurring spend, utilizing `formData.wallets` as options.
- [ ] Task 2.2: Bind the dropdown in `StepRecurringExpenses.tsx` to `updateRecurringSpendWalletMapping` on change.
- [ ] Task 2.3: Update `StepReview.tsx` to display the selected wallet name from the mapping rather than the default from the global recurring spend model.

### Verification

- [ ] Open the Period modal, navigate to Step 3 (Expenses), and verify that each recurring spend has a functional wallet selector dropdown displaying the wallets configured in Step 2.
- [ ] Proceed to Step 4 (Review) and verify that the selected wallet name matches what was chosen in Step 3.

## Phase 3: Firestore Submission Integration

Persist the selected wallet mappings to the database when the period is finalized.

### Tasks

- [ ] Task 3.1: Import `useUpdateRecurringSpend` hook in `PeriodModalV2.tsx`.
- [ ] Task 3.2: Update `PeriodModalV2.tsx`'s `handleFormSubmit` function to update the mapped recurring spends in Firestore before finalizing period creation. Iterate over `recurringSpendsWalletMapping` and update each recurring spend's `walletName` in the global `recurringSpending` subcollection.

### Verification

- [ ] Complete the period wizard, select specific wallets for recurring spends, click create, and verify that:
  - The global recurring spends' `walletName` field is updated in Firestore.
  - The auto-generated spends for the new period are assigned to the correct wallets.

## Final Verification

- [ ] All acceptance criteria met
- [ ] Tests passing
- [ ] Documentation updated (if applicable)
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
